package com.example.demo.repositories.candidato;

import com.example.demo.entities.candidato.CandidatoCV;
import com.example.demo.repositories.BaseRepository;

public interface CandidatoCVRepository extends BaseRepository<CandidatoCV, Long> {
    // Define any additional query methods specific to CandidatoCV if neede
} 
